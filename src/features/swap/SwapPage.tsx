import React from 'react';
import SwapForm from './components/SwapForm';

const SwapPage: React.FC = () => {
    return (
        <div className="max-w-7xl mx-auto px-4 py-20 min-h-[600px] flex items-center justify-center">
            <SwapForm />
        </div>
    );
};

export default SwapPage;
